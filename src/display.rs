#[allow(dead_code)]

mod gutter;
mod forest;
pub mod displayable;
pub mod builder;

#[cfg(test)]
mod tests {

    use super::*;

    #[test]
    fn multi_struct_tree() -> Result<(), String> {
        Ok(())
    }
}
